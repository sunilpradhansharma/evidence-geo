"""Activation & Impact — the operational loop that turns a GEO recommendation into an
owned, published intervention and measures the before/after change in AI answers.

Thin v1: measurement (KPI snapshots + single-arm before/after result) reuses the existing
run + scoring layers; there is no separate analytics engine. See the plan for scope.
"""
