"""Source Authority Mapping (FR-706a).

Extracts the publishers an AI model cites, classifies each domain on two independent axes
(ownership + authority) via a curated taxonomy with optional WhoisXML enrichment, and powers
the Source Authority dashboard (distribution, top-cited per model, alerts, preferred-source
tracking). See the individual modules for detail.
"""
